umbrel-app.yml
manifestVersion: 1
id: xxxx-blockstream-satellite
category: bitcoin
name: Blockstream Satellite
version: "1.0.0"
tagline: Access Bitcoin via Blockstream Satellite
description: >-
  Blockstream Satellite enables Bitcoin blockchain data synchronization via satellite without requiring an internet connection.
developer: Your Name or Organization
website: https://blockstream.com/satellite/
dependencies:
  - bitcoin
ports:
  - 3000

version: '3.7'

services:
  blockstream-satellite:
    image: your-docker-image-name # Replace with the Docker image for Blockstream Satellite or create one
    container_name: blockstream-satellite
    ports:
      - "3000:3000"
    environment:
      - API_KEY=${API_KEY} # Use environment variables for sensitive data
    volumes:
      - ./data:/data # Persistent storage for satellite data
